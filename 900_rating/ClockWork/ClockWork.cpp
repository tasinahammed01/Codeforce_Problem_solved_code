#include <bits/stdc++.h>
using namespace std;

int main()
{

    int t;
    cin >> t;

    while (t--)
    {
        long long n;
        cin >> n;

        vector<int> s(n);

        for (int i = 0; i < n; i++)
            cin >> s[i];
        
        
    }

    return 0;
}